#include <iostream>
#include <string>
using namespace std;
void calculate() {
        string s;
        int count = 1;  
        cout << "\nEnter a string: ";
        getline(cin, s);  
        for (size_t i = 0; i < s.length(); i++) {
            if (s[i] == ' ') {  
                count++;
            }
        }
    cout << "\nNumber of words: " << count << endl;
}
int main() {
    n.calculate();
    return 0;
}
