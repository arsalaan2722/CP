#include <iostream>
using namespace std;

int main() {
    int P, count = 0;

    cout << "Enter a number: ";
    cin >> P;

    if (P == 0) {
        cout << 1;
        return 0;
    }

    while (P != 0) {
        count++;
        P = P / 10;
    }

    cout << count;
    return 0;
}
