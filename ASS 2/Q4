#include <iostream>
using namespace std;

int main() {
    int N, sum = 0;

    cout << "Enter a number: ";
    cin >> N;

    while (N != 0) {
        sum += N % 10;
        N /= 10;
    }

    cout << sum;
    return 0;
}
